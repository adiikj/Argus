# Normalize line endings to LF in the repo (important on Windows + Docker/Linux containers).
* text=auto eol=lf

# Keep binary/lockfiles from line-ending munging.
pnpm-lock.yaml -text
*.png binary
*.jpg binary
*.ico binary
